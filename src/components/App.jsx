import { Button } from './Button/Button';

export const App = () => {
  return (
    <div>
      <Button text={'Good'} />
      <Button text={'Neutral'} />
      <Button text={'Bad'} />
    </div>
  );
};

export default App;
